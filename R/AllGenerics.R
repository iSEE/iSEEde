setGeneric(
  "pvalue",
  function(x, ...) standardGeneric("pvalue")
)

setGeneric(
  "logfc",
  function(x, ...) standardGeneric("logfc")
)

setGeneric(
  "average",
  function(x, ...) standardGeneric("average")
)
